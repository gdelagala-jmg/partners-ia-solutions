# Security Hardening: Prevent execution of script files in uploads directory

# Disable script execution engine for common web extensions
<FilesMatch "\.(php|php5|php7|php8|phtml|pl|py|jsp|sh|cgi)$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

# Prevent directory indexing / browsing
Options -Indexes
